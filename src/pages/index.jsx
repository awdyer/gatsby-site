import React from "react";
import Header from "../components/header";

export default () => (
    <div style={{ color: `brown` }}>
        <Header />
        <h1>Sup</h1>
        <p>friend.</p>
    </div>
);
